@model IEnumerable<SAC_Web_Application.Models.ClubModel.Qualifications>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QualName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QualName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.QualID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.QualID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.QualID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
